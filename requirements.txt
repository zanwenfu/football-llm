# Football-LLM dependencies
#
# This file is kept for backwards compatibility. The canonical source of truth
# is pyproject.toml. Prefer:
#
#   pip install -e ".[dev]"           # reproduce the paper + run tests
#   pip install -e ".[train]"         # fine-tuning (GPU required)
#   pip install -e ".[serve,vllm]"    # full serving stack (GPU required)
#   pip install -e ".[all]"           # everything
#
# The lines below install the everything-bucket and are equivalent to `.[all]`.
-e .[baselines,train,serve,dev]
